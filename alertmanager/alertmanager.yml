route:
  receiver: "default"
  group_by: ["alertname", "instance"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: "default"
    # Wire a real notifier here (Slack, email, or webhook), e.g.:
    # slack_configs:
    #   - api_url: "https://hooks.slack.com/services/XXX"
    #     channel: "#alerts"
